// Onur
// Code Forces 282A
// c
#include <stdio.h>
int main() {
    int t,x=0;
    scanf("%d",&t);
    while(t--)
    {
        char a[4];
        scanf("%s",a);
        if(a[1]=='+')
        x++;
        else
        x--;
    }
    printf("%d",x);
    return 0;
}
